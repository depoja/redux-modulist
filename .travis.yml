language: node_js
node_js:
  - "lts/*"

cache:
  yarn: true
  directories:
    - node_modules

jobs:
  include:
    - stage: test
      script: yarn
      script: yarn test
    - stage: build
      script: yarn
      script: yarn build
